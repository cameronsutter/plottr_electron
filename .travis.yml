osx_image: xcode7.3
os: osx
language: node_js
node_js:
  - "7"
env:
  - TRIALMODE=false NODE_ENV=production ELECTRON_CACHE=$HOME/.electron
  - TRIALMODE=trial NODE_ENV=production ELECTRON_CACHE=$HOME/.electron
cache:
  directories:
  - node_modules
  - $HOME/.electron
  - $HOME/.cache
script:
  - npm run dist
branches:
  only:
    - master
