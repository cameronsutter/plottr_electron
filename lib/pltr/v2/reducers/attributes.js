const INITIAL_STATE = []

const attributesReducer =
  (dataRepairers) =>
  (state = INITIAL_STATE, action) => {
    switch (action.type) {
      default: {
        return state
      }
    }
  }

export default attributesReducer
