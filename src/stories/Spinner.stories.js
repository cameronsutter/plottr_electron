import React from 'react'

import { Spinner } from '../components/Spinner'

export default {
  title: 'Plottr/Spinner',
  component: Spinner,
  argTypes: {},
}

const Template = () => <Spinner />

export const Example = Template.bind({})
Example.args = {}
